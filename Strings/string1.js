//Traverse your name and print its length, indices, and each character.

let name = "Shairyar"
console.log(name.length);
console.log(name[6]); 

for(let i=0; i < name.length; i++) {
    console.log(name[i] , "all string character");
}
