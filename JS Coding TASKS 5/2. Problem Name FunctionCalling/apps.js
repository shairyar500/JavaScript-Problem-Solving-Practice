/*
Using the function you have created in previous question, call the function multiple times with different parameters.
*/

function double(paisa, coins, rupees) {
    let answer = (paisa * 2);
    return answer;
}

console.log(double(10));